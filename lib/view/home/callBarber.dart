import 'package:flutter/material.dart';

class CallBarber extends StatelessWidget {
  const CallBarber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(


    );
  }
}
